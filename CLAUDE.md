# 給 Claude 的工作筆記

這個 repo 目前的主角是 `moon-jelly/`「海月水母館」：療癒的水母缸網站，把心情記下來會長成海裡的生物（功能與檔案見 `moon-jelly/README.md`）。
這份筆記只記專案檔案裡沒有的東西：使用者的習慣、召集過的專家與他們的要求、我的做法。

## 使用者的習慣

- 用繁體中文溝通。常在手機上看結果（Claude app、GitHub 手機版），所以回報要短、分段清楚，截圖比文字有用。
- 常用「請繼續」「繼續」推進。會給一整個階段讓我自己做完（例如「把 P0~2 全完成再跟我確認」），**做完一定要回報並等確認**，不要自己跳到下一個大方向。
- 要求「記得清楚的檢查」：每一輪都要實際跑測試，並用截圖親眼看過桌面與手機的畫面，不能只看程式碼就說完成。
- 喜歡先請「專家團隊」從不同角度分析，再整合成計畫；也接受多個 agent 平行施工，但一定要**風格統一、有設計感**。
- 在意的方向：背景和顏色會影響情緒；不要「明顯的 AI 風格」；鼓勵記錄心情是好的；「光」要稀有；心理安全優先。
- **心情紀錄遺失是重大事故**：任何會丟紀錄的邏輯（上限、清除、同步覆蓋）都不能有；要做帳號與雲端備份。
- 已拍板的方向都記在 `moon-jelly/docs/plans/decisions.md`（綠界、小額、不訂閱、不 iOS、第一批商品先不上、多語言先只規劃），不要再問。
- **對歷史非常謹慎**：任何改動的 commit 細節都要保留，做不到要先確認。不要改寫歷史、不要 force push；要保存就另開分支。
- 做決定的方式：自己把新舊兩版打開逐項對照，再用編號告訴我要留哪些。給他的清單要編號、寫出新舊差別和「在哪裡看」。
- 從新版挑東西時，他要的是**「元素與精神」，不是照搬**：例如「說明牌用細線指著生物」這個想法留下，但牌子本身的樣式要照舊版（霧面玻璃、圓角、手寫字）。一次只給他一小部分，做完讓他看。
- 新的改動先發到**另一個**預覽網址讓他比對，他確認 OK 才部署到他最初的網站（B2rN）。

## 目前狀態

- `main`：重新設計**之前**的介面（光變稀有、依心理學引導、棲地商店、可直接撥打的危機專線）。
- `claude/moon-jelly-redesign-ba`：B+A 介面重新設計的完成版，只當參考。規格在 `moon-jelly/DESIGN.md`，四份專家報告和審查圖在 `moon-jelly/docs/`。要看全部差異：`git diff main claude/moon-jelly-redesign-ba -- moon-jelly`。
- `claude/creative-healing-website-8lqski`（工作分支，PR #2）：從 54 項差異清單挑回來、用舊風格重做的東西，每一項一個 commit：歡迎卡標題「入館」、左上角縮小、餵食選單變矮、**生物說明牌**（`js/callout.js`、`css/callout.css`：圈＋細線指著生物、牌子靠上或靠下不擋牠、往空處拉＝詳細、往邊緣拉＝關、左右滑換一隻）、陪完心情／出生／撈到／長大等通知之後牌子貼到那隻生物旁邊（第 18 項水族箱小事件**不要**細線，使用者要自己找）、第 4 步文案「現在大概在哪裡？」加「剛才」淡刻度（滑桿與浪照舊）。
- 待辦：使用者還在從 54 項差異清單（A 整體、B 開場與主畫面、C 生物說明牌、D 心情儀式、E 抽屜、F 紙、G 水族箱、H 文字）裡挑；只把他選的搬回來，每一項單獨成 commit。另外召集過兩組專家（日夜差異更明顯、金流／App／營利），報告放 `moon-jelly/docs/`。
- 預覽頁（都只有使用者看得到）：最新改動的比對用 https://claude.ai/artifact/MSyocsFgAuFb5TKRKJZ5i7（同一個檔案路徑重發就會更新）；重新設計 https://claude.ai/artifact/DFG6X5wVXbGxf3VNLGH1s7；使用者最初的網站 https://claude.ai/artifact/B2rNPPbEHxDQTRhYkoeBeR（停在危機專線改版之前；要更新它得先用 Artifact read 把整份讀完才能發布）。舊版 main 的預覽已依使用者要求刪除。

## 召集過的專家與他們的要求

1. **遊戲設計＋心理學團隊**（心情生態系）：每種陪伴方式長成一種生物、生物之間有關係；之後的分析產生 P0–P2：
   - P0 光變稀有：每份心情給的光都一樣（每天前三份各 30），不因選了哪種陪法、浪有沒有變小而不同；能刷光的地方都收起來。
   - P1 依心理學引導：陪法依強度排序、只推薦一個、新增「先著陸」（5-4-3-2-1）、只想倒出來的快速路徑、連續只倒出來時輕輕提醒。
   - P2 棲地商店、本週回顧、提醒記錄的小點。都已在 main。
2. **國際 UX／美術審查**（「看起來像 AI 做的」診斷）：找出 13 個 AI 痕跡（膠囊按鈕、毛玻璃、置中對話框、彩虹圖示、發光字、過多強調色…），提出 A 標本圖版、B 夜間水族館展示牌、C 航海手帳三個方向；使用者選 B+A。
3. **B+A 規劃小組**（四位，報告全文在參考分支 `moon-jelly/docs/panel/`）：
   - 美術指導：海是展廳、紙是你留下的東西；直角、細線、左對齊；說明牌貼在生物旁邊用細線指過去；拿掉介面圖示。
   - 色彩與情緒：亮度與飽和度比色相更影響情緒；只留一個強調色；負面情緒的顏色降彩度不壓暗；夜裡（21–07）總光量要降；不用紅綠表示好壞。
   - 臨床 UX（心理安全，優先）：兩層選字、強度預設不填、浪大時只列少數陪法、陪法清單不放生物圖、第 4 步是觀察不是測驗、不獎勵浪變小、不做連續天數。
   - 字體與文案：只用 6 級字、中文字距 0；手寫體只給使用者自己打的句子；不用驚嘆號、「・」、「— 海」署名。
4. **不論介面怎麼改都要守住的安全規則**（已在 main）：每個寫字的地方都做自傷字詞檢查；危機畫面列出可直接撥打的 1925、1995、113、1980（不是 24 小時，排最後）和 119／110；危機紀錄不給光、不重現原文、之後整段安靜；危機元件不套美術風格。

## 我的做法（專案裡沒寫的）

- **功能檔**：`moon-jelly/docs/features/` 每個功能一個檔案（一句話、已有、待開發、決定過的事），`README.md` 是總表。改功能前先讀那個檔，做完更新它；專家報告全文在 `docs/panel/`，規劃書在 `docs/plans/`。這樣不用讀程式就能回答「現在有什麼」。
- **分工**：使用者會用另一個（本機的）session 實作 `moon-jelly/docs/specs/` 裡的規格；雲端 session 負責規劃、規格、審查與預覽。規格要寫到「換一個沒看過對話的人也能做」：入口、資料、文案、驗收。
- **部署**：Artifact 擋所有網路請求（只放行幾個 script CDN 與 Google Fonts），所以有帳號與雲端之後正式網站要放 GitHub Pages；Artifact 只當預覽。

- **大改之前**：先請專家各寫報告 → 整合成一份規格（寫明衝突怎麼裁決）→ 主設計者先做共用基礎（token、元件）→ 多個 agent 依**嚴格的檔案分工**在各自的 git worktree 平行施工 → 整合、一致性掃描、完整回歸測試、親眼看截圖。agent 可能撞到用量上限，要他們每完成一段就先 commit 存檔。
- **測試**：Playwright（`/opt/node22/lib/node_modules/playwright`，不要 `playwright install`）＋ `http-server -p 8765 -s -c-1 moon-jelly`。回歸測試分四組：桌面（50 項）、手機 390×844（9 項）、危機安全（24 項）、舊存檔相容；另外用 `file://` 測單檔版。這些腳本放在 session 的暫存區，換 session 要重寫。
- **字型**：沙盒裡的 Chromium 連不到 Google Fonts。用 curl 把字型下載到本機，再用 Playwright 的 `route` 攔截 fonts.googleapis.com／fonts.gstatic.com，回傳本機檔案。
- **預覽**：`node moon-jelly/tools/build-single.js <輸出> --fragment` 產生片段，發布成 Artifact；舊版和新版要用不同的檔案路徑，才不會互相覆蓋。
- **commit**：英文、祈使句、說明「為什麼」；附 Co-Authored-By 與 Claude-Session 兩行；不寫模型名稱；絕不改寫已推送的歷史。
- **回報**：繁體中文、白話、短；先講結果，再列「需要你確認的事」。
